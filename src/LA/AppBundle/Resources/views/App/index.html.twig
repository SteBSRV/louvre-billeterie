{% extends "LAAppBundle::layout.html.twig" %}

{% block title %}
  Page d'accueil - {{ parent() }}
{% endblock %}

{% block body %}

  <h2>Accueil</h2>

  {% for type, messages in app.session.flashbag.all %}
    {% for message in messages %}
      {%if type == 'error'%} {% set type = 'danger' %} {%endif%}
        <div class="alert alert-{{ type }}">
            {{ message|raw }}
        </div>
    {% endfor %}
  {% endfor %}

  <div class="panel panel-default">
  	<div class="panel-heading">
  	  <h3 class="panel-title">Bienvenue</h3>
  	</div>
  	<div class="panel-body">
  		<p style="text-indent: 60px;">
  			Bienvenue sur le site du Musée du Louvre, vous trouverez ici les informations sur le musée (tarifs, horaire, contact etc...), ainsi que la billetterie en ligne pour vous permettre d'arriver directement au Musée avec vos billets. Nous vous souhaitons une bonne visite sur notre site, en espérant vous retrouvez prochainement au musée.
  		</p>
  	</div>
  </div>

{% endblock %}
